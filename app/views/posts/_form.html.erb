<%= form_for @post do |f| %>
  <%= f.label :user_id %>
  <%= f.collection_select :user_id, User.all, :id, :username %>
  <!-- image upload -->
  <%= f.label :description %>
  <%= f.text_field :description %>

  <%= f.submit %>
<% end %>
